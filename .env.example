# Copy to .env for local development. Never commit the real .env.
#
# MongoDB Atlas M0 (free forever). Two things that bite people:
#   1. Put the database name in the path, BEFORE the "?" — omit it and Mongo
#      silently writes to a database called "test".
#   2. Use an alphanumeric-only password. Any of @ : / ? # [ ] % must be
#      percent-encoded in the URI, which is a miserable thing to debug.
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/cricket?retryWrites=true&w=majority

# Local dev port. 3000 is a busy default that other projects tend to claim,
# and on Windows a second app bound to [::1]:3000 will shadow this one for
# browsers, because localhost resolves to IPv6 first. Render injects its own
# PORT, so this only affects development.
PORT=3100

# Signs session tokens. MUST be set in production — without it the server
# generates a random one at boot and every restart logs everybody out.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-a-long-random-string
